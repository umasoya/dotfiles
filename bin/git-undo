#!/bin/bash
#vim: ft=sh

for OPT in "$@"
do
  case "${OPT}" in
    '-s'|'--soft')
      option="--soft"
      ;;
    '-h'|'--hard')
      option="--hard"
      ;;
    *)
      back=${OPT}
  esac
done

git reset ${option:=--soft} HEAD~${back:=1}
